# Node: Batch 1 Storage Interface

Implement only the shared async session-storage CRUD contract, adapt SQLite to
it without changing the default path or behavior, and add a non-zero contract
suite against temporary SQLite. Record deterministic evidence before batch 2.
